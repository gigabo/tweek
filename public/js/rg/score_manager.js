// Generated by CoffeeScript 1.8.0
(function() {
  require.def(['rg/score_time', 'rg/debug'], (function(_this) {
    return function(ScoreTime, Debug) {
      var ScoreManager, all_scores;
      all_scores = [];
      return ScoreManager = (function() {
        function ScoreManager(game) {
          this.game = game;
        }

        ScoreManager.prototype.types = function() {
          return ['time', 'finish', 'loops', 'total'];
        };

        ScoreManager.prototype.register = function(score) {
          return all_scores.push(score);
        };

        ScoreManager.prototype.reset = function() {
          return all_scores = [];
        };

        ScoreManager.prototype.total = function() {
          var score, total, _i, _len;
          total = 0;
          for (_i = 0, _len = all_scores.length; _i < _len; _i++) {
            score = all_scores[_i];
            total += score.value();
          }
          return total;
        };

        return ScoreManager;

      })();
    };
  })(this));

}).call(this);
